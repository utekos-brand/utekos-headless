/**
 * Disabled until Usercentrics autoblocker can safely ignore Next.js bootstrap
 * scripts and JSON-LD. Consent-gated loading is handled by explicit gates.
 */
export function UsercentricsAutoblockerScript() {
  return null
}
