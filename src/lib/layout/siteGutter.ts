/**
 * Shared horizontal inset for site chrome (header wordmark, breadcrumbs, etc.).
 * Keep header and breadcrumb trails on the same x-axis.
 */
export const siteXGutterClassName = 'px-4 sm:px-6 lg:px-10'
