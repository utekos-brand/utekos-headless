// Path: src/lib/constants/cookies.ts

/**
 * @fileoverview Centralized constants for application-wide cookie names.
 * Using constants for cookie names prevents typos and ensures consistency
 * across the codebase where cookies are set and read.
 */

/**
 * The name of the cookie used to store the Shopify cart ID.
 */
export const CART_COOKIE_NAME = 'cartId'
