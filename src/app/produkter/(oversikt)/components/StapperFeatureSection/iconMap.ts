import { Minimize2, Feather, ShieldCheck, Settings2 } from 'lucide-react'
export type IconName = keyof typeof iconMap
export const iconMap = {
  'minimize-2': Minimize2,
  'feather': Feather,
  'shield-check': ShieldCheck,
  'settings-2': Settings2
}
